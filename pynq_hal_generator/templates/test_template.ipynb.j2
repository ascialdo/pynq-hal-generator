{
 "nbformat": 4,
 "nbformat_minor": 5,
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.8.0"
  }
 },
 "cells": [
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Cell 1: imports\n",
    "import sys\n",
    "sys.path.insert(0, '{{ submodule_path }}')\n",
    "\n",
    "import pynq\n",
    "import numpy as np\n",
    "from {{ output_package }}.hal import {{ config.ip_name | pascal_case }}"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Cell 2: load overlay\n",
    "ol = pynq.Overlay('design_1.bit')"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Cell 3: instantiate HAL\n",
    "dut = ol.{{ config.ip_name }}_0\n",
    "print(type(dut))"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Cell 4: reset and dump registers\n",
    "dut.reset()\n",
    "print(repr(dut))"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Cell 5: run all built-in RW tests\n",
    "results = dut.run_all_tests()\n",
    "passed = sum(results.values())\n",
    "total = len(results)\n",
    "print(f'Summary: {passed}/{total} tests passed')"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Cell 6: Custom Tests\n",
    "## Add your custom test cases here\n"
   ]
  }
 ]
}
